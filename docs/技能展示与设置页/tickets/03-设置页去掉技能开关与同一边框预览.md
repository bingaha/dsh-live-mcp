# 03 — 设置页：去掉技能开关、同一边框预览、过滤

**要做的事：** 设置页技能从「假启用开关」改成只读展示作者策略。用户点一条技能时，预览在同一条边框里向下长高，而不是旁边再冒一张卡。改 MCP 或导入/删除技能后，通知当前窗口的状态条重拉。

**改动面：** 修改 `src/client/manager.tsx`、`src/client/settings-card.module.css`

**阻塞于：** 00 — 同页能力变更通知模块；01 — 技能调用策略进入列表与会话 API

**状态：** 已完成

- [x] 技能行没有启用/禁用开关，也不再调用技能 toggle API
- [x] 状态点与状态条同一套绿/黄/橙/红（不再用绿/红表示插件开关）
- [x] 有限制才显示只读标签「模型不可用」「用户不可用」；两轴都能调不标「正常」；两轴都关则两个标签都挂
- [x] 过滤为「全部 / 模型不可用 / 用户不可用」；两轴都关的技能在后两个过滤里都能看到；名称搜索保持
- [x] 点整行主区域或「详情」展开，再点收起；同时只展开一条
- [x] 展开后预览（描述、When to use、正文）与该技能名称行、来源、删除按钮处于同一个边框，边框随内容变高；不再另起独立边框的详情块
- [x] 删除按钮仍可点到且不误触展开
- [x] 导入/删除技能、MCP 保存/启停/删除/重试成功后发布 00 的同页通知（不轮询）
- [x] MCP 面板的启用开关与连接状态展示保持原样（本工单不改 MCP 启停语义）
