# 04 — README 与用户说明对齐

**要做的事：** 用户按 README 理解本插件时，不应再看到「技能黑名单会不注入」「设置页技能开关走 state.json」这类已被本轮取消的说法。文档与状态条颜色、设置页只读策略、MCP 仍可按会话屏蔽对齐。

**改动面：** 修改 `README.md`

**阻塞于：** 02 — 状态条：四色只读技能 + MCP 短文案；03 — 设置页：去掉技能开关、同一边框预览、过滤

**状态：** 已完成

- [x] 不再把技能写成可点黑名单、不再声称点技能会从本会话拿掉工具/注入
- [x] 不再把设置页技能启用/禁用写成插件能力（本轮无此开关）
- [x] 写明技能 chip 只读、四色含义，以及设置页只读「模型不可用 / 用户不可用」
- [x] 写明 MCP 仍可按会话屏蔽、状态条无「黑名单 / 全部可用 / 移出」字样
- [x] 仍说明技能启停不写 SKILL.md、不控制 `disable-model-invocation`
