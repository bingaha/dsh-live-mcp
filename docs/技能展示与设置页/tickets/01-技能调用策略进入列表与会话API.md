# 01 — 技能调用策略进入列表与会话 API

**要做的事：** 设置页和状态条要按作者策略上色，不能再把 `disable-model-invocation` 当成插件「禁用」，也不能再用 `state.json` 旧开关过滤技能。本工单打通对外 JSON：技能列表/详情以及会话能力 GET 都带上「模型可调用 / 用户可调用」，缺省与 DSH 一致。

**改动面：** 修改 `src/protocol.ts`、`src/skills.ts`、`src/routes.ts`、`src/index.ts`、`src/client/api.ts`；新增 `src/skills.test.ts`

**阻塞于：** 无——可立即开始（可与 00 并行）

**状态：** 已完成

- [x] 技能列表与详情带有模型可调用、用户可调用两个布尔值
- [x] `disable-model-invocation: true` 只让模型可调用为假，不把该技能从列表拿掉
- [x] 未写调用字段的技能两条都为真（与 DSH 缺省一致：未写 DMI = 模型可调用；未写 `user-invocable` = 用户可调用）
- [x] 插件状态文件里的旧技能开关键不覆盖、不过滤上述结果（忽略不删）
- [x] 会话能力 GET 的技能候选项从「一串名字」改为「名字 + 模型可调用 + 用户可调用」，且不因旧开关过滤
- [x] 会话能力 GET 的 MCP 候选项仍是全局已启用服务器 + 连接状态（行为保持）
- [x] 系统提示公告不再声称「技能启用/禁用走插件自有开关集合」
- [x] 自动化打在 REST / 解析边界：覆盖 DMI、user-invocable、缺省、旧开关不生效
