# 02 — 状态条：四色只读技能 + MCP 短文案

**要做的事：** 对话框上方状态条不再看起来像一份黑名单。技能 chip 只展示作者策略（绿/黄/橙/红、点不动）；MCP 仍可点屏蔽，但标题和悬停不再出现「黑名单」「全部可用」「移出」。本窗口设置页改完能力后，状态条马上重拉。

**改动面：** 修改 `src/client/ConversationDock.tsx`

**阻塞于：** 00 — 同页能力变更通知模块；01 — 技能调用策略进入列表与会话 API

**状态：** 已完成

- [x] 无总标题「黑名单」「全部可用」；保留分组「MCP」「技能」
- [x] 无「移出：xxx」行；会话文件里残留的名字不占状态条（以后同名再启用仍按已屏蔽显示为灰 MCP）
- [x] 技能 chip 不可点；颜色：绿=两轴都可调用，黄=仅模型不可调用，橙=仅用户不可调用，红=两轴都不可调用
- [x] 技能 tooltip 短状态、不写「点击…」、不写「黑名单」：可用 / 模型不可用 / 用户不可用 / 模型不可用，用户不可用
- [x] MCP chip 仍可点切换本会话屏蔽；颜色保持绿/灰/红/琥珀（灰压过连接态；红失败仍可点成灰）
- [x] MCP tooltip 短状态：可用 / 已屏蔽 / 连接失败 / 连接中
- [x] 订阅 00 的同页通知后重拉当前会话视图；不轮询
- [x] 技能候选项使用 01 提供的两个调用标志上色，不再按插件开关过滤、不再按会话技能黑名单画灰
