# 04 — 状态条（输入框上方）

**要做的事：** 在聊天输入框上方新增一条状态条（槽位 `conversation.input.dock`），展示并管理**当前对话**的 MCP 与技能集合（选中/未选中、连接状态）。会话**未产出内容**时可自由增删其能力（写入会话选用并触发隔离装配），**产出后只读**（真隔离锁定）；可选集合只含全局已启用的能力。

**阻塞于：** 03（选用数据）、01（隔离装配行为）

**状态：** 待代理执行

- [ ] 状态条渲染在输入框上方，展示当前会话的 MCP 与技能集合及连接/启用状态（样式参考既有面板）
- [ ] 会话未产出内容时可增删其能力，并把选用写入会话、触发按会话装配
- [ ] 会话产出后状态条进入只读展示，不再改真隔离集合
- [ ] 状态条可选集合只包含「全局已启用」的 MCP 与技能
