# 02 — 全局启停改造（含设置页）

**要做的事：** 把「技能启用/禁用」从改写 `SKILL.md` 的 `disable-model-invocation` 改为**插件自有开关集合**承载（彻底移除对 disable 属性的写入与读取支持）；MCP 全局启用/禁用沿用 `mcp.json` 的 `enabled`。提供 REST API 读写全局启停并持久化（重启保留）；设置页面向用户基本不变的技能开关改走新的开关集合。

**阻塞于：** 无——可立即开始，与 01 并行。

**状态：** 待代理执行

- [ ] 技能的启用/禁用不再写/读 `disable-model-invocation`，改由插件自有开关集合承载并持久化到插件 state 文件
- [ ] 全局启停集合经管理 REST API 可读写，重启后保留
- [ ] MCP 全局启用/禁用沿用 `mcp.json`，语义维持现状
- [ ] 设置页技能的启用/禁用改走新开关集合，页面面向用户基本不变
