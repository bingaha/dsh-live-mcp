# 03 — 会话黑名单存储与按会话解析

**要做的事：** 让每个会话拥有自己的「能力黑名单」：某个会话可选用「屏蔽哪些 MCP/技能」。会话黑名单写入**该会话自己的目录**（有 session 就有配置、删 session 随会话清理）。管理 REST API 支持按会话增删黑名单，并返回该会话**解析出的可用集合**——空黑名单 = 见全局已启用的全部；非空 = 全局启用中除黑名单外全部可用。已启用服务器的 `available` 选项额外带**实时连接状态**（供状态条把连接失败的标红）。

**阻塞于：** 02（需要以全局启停集合作为「可选/可用」的数据源）

**状态：** 已完成（随 07a950b 之后的迭代实现）

- [x] 会话的黑名单（屏蔽的 MCP/技能名单，空 = 全可用）写入该会话自身目录，随会话生命周期存在与清理
- [x] 管理 REST API 支持按会话增删黑名单（MCP 与技能）
- [x] 管理 REST API 返回某会话解析出的可用集合，且已启用 MCP 选项携带连接状态（failed→红）
- [x] 只有「全局已启用」的能力才可作为会话黑名单的可选来源（红色 failed 服务器仍可按名配置）
