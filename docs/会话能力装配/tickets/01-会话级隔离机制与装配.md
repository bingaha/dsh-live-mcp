# 01 — 会话级黑名单机制与装配

**要做的事：** 让「按会话的黑名单」真实生效：不改 DSH 源码、用 DSH 既有机制证明「某会话对某启用能力可以不可见」——把某服务器加入会话黑名单后，它的 `mcp__` 工具被 deny，不进该会话 schema/上下文；不在黑名单则全部可用（等价于无配置）。打通「会话 A 屏蔽了 X、会话 B 没屏蔽 X 而可见」这条端到端行为。

**阻塞于：** 无——机制验证部分最前沿、可立即开始；装配接入部分依赖 03（需要会话黑名单数据）。

**状态：** 已完成（随 07a950b 之后的迭代实现）

- [x] 不改 DSH 源码，验证某会话的 scope 可只屏蔽黑名单服务器，其 `mcp__` 工具不进 schema/上下文
- [x] 空黑名单会话继承全局、看到全部已启用能力（= 无配置、历史会话不被切断）
- [x] 黑名单会话：创建时按落盘黑名单装配；之后状态条改动经 applyLive 实时应用到活 agent（dispose 旧 restrict → 挂新 restrict），改动下次请求生效
- [x] 已启用但连接失败（failed）的服务器在状态条标红，且仍可按服务器名配置黑名单
- [x] 两个会话（一黑名单一空）对同一启用能力的可见性不同，端到端可演示
